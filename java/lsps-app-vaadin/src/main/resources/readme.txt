${project.name}
${project.description}

${project.version} as per commit ${buildNumber}

This application depends on ${project.parent.parent.description} (${project.parent.parent.name}) ${lsps.release.version} (patch version ${project.parent.parent.version})

